# USER

Semantic model of the Owner — preferences, patterns, communication
tells, decision habits. Hard ceiling: **100 lines**. Consolidate
existing entries before append when at 80% capacity (≥80 lines).

Loaded at session-start (item 5 of read order). Frozen-at-session-
start — entries written here appear in next session, not current.

## Preferences

(Decision/style/format preferences the Owner consistently shows.
Example: prefers menu-format options over open-ended questions; picks
fix-all over per-item ratification for trivial fixes.)

## Patterns

(Recurring workflows or framings the Owner uses. Example: starts
with research → narrows to options → picks → ratifies plan → executes.)

## Tells

(Communication signals — what specific phrasings mean. Example: "fix
all" means exact list above, no scope creep; "do (a)" means take
option a verbatim; silence on options usually means "you decide".)
